%h1 Edit User "#{@user.full_name}"

= form_for(@user) do |f|

  = f.text_field :first_name
  = f.text_field :last_name
  = f.email_field :email
  = f.select :country_code, collection: ISO3166::Country.all.map { |c| [c.translations["en"], c.alpha2] }

  = f.submit "Save"
